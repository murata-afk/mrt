# SYSTEM.md — 課題1-4 裏側システム仕様

## 何を作るか（1行）
MELTY HARAJUKU の公式サイトに届く「お問い合わせ・予約」を、AIが自動採点して3段階に振り分け、
人の承認ゲートを通して返信ドラフトまで用意する社内トリアージ管理ツール（動くモック）。

## 成果物
- 全体像の図解（admin.html 内に SVG で描画）
- システム本体 admin.html（1ファイル完結・localStorage保存・日本語・白ベース）

## データモデル（localStorage）
- キーは `melty.triage.*` で統一
  - `melty.triage.items` … 問い合わせ配列
  - `melty.triage.audit` … 操作ログ（誰がいつ何を承認/却下したか）
  - `melty.triage.settings` … しきい値・返信テンプレ
- item: {id,name,email,type,channel,message,createdAt,score,tier,stage,reasons[],reply}

## スコアリング（100点満点・自動）
加点要素: 連絡先の充実 / 予約・取材など優先タイプ / 緊急語 / 具体性(文字数) / リピーター
減点要素: 迷惑メール語 / 情報不足
- 80〜100 = 自動でOK（auto）
- 50〜79 = 人が確認（review）
- 0〜49 = 却下候補（reject）

## 引き継ぎ型ゲート（人が承認してから次段階）
受信 → 自動トリアージ → [承認ゲート:人が承認] → 返信ドラフト生成 → [人がドラフト承認] → 送信済(モック)
- 却下も人の操作。すべて監査ログに記録。
- 確認は自作モーダル（ブラウザ標準 confirm 不使用）。

## 画面（5タブ）
1. ダッシュボード（KPI・tier分布・全体像SVG図解）
2. 受信トレイ（トリアージ結果一覧・フィルタ・詳細）
3. 承認ゲート（人の承認待ち＝次段階へ引き継ぐ関門）
4. 返信ドラフト（自動生成文面の確認・承認で送信済へ）
5. 設定 & AIヒント（しきい値・テンプレ・各画面のAI活用ヒント）

各画面に「この画面でAIに頼むと良いこと」ヒントを常設。サンプル12件を初期投入。
